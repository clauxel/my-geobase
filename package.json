{
  "name": "veovido",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-site.mjs",
    "dev": "npm run build && wrangler pages dev public",
    "deploy:pages": "npm run build && wrangler pages deploy public --project-name=my-veovido",
    "deploy:worker": "npm run build && wrangler deploy --keep-vars --config wrangler.worker.toml"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
